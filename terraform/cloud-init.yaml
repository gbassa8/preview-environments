#cloud-config
hostname: athenea
package_update: true

runcmd:
  - curl -fsSL https://tailscale.com/install.sh | sh
  - systemctl enable --now tailscaled
  - tailscale up --auth-key=${tailscale_auth_key}
  - curl -sfL https://get.k3s.io | INSTALL_K3S_EXEC="server --tls-san athenea" sh -
